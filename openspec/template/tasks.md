## 1. 规格

- [ ] 1.1 更新 `changes/<本变更名>/specs/*/spec.md`（Requirement + Scenario）
- [ ] 1.2 合并入 `openspec/specs/*/spec.md` 并更新 Purpose

## 2. 实现与验证

- [ ] 2.1 仓库根 `src/`、`templates/`、`assets/`、`hub/` 等与规格一致的代码
- [ ] 2.2 仓库根执行 `npm test`
- [ ] 2.3 README / CHANGELOG 如需对外说明则同步

## 3. 收尾

- [ ] 3.1 勾选本文件全部任务
- [ ] 3.2 （可选）将已定稿的 `changes/<短名>/` 整目录移入本机或团队约定的归档位置；本仓库默认 **不** 跟踪 `openspec/changes/archive/`（见根 `.gitignore`）
